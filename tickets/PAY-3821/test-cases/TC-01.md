Validate that Treasury API health endpoint returns Ok before funding-batch testing

**AC Linked:** PAY-3821 / setup (supports AC-3 service-health assertions)

**AC Description:** Treasury API is reachable and reports healthy before failure-path testing begins.

**Preconditions**

- STAGE Treasury API deployed (`stage-wf-treasury-api`).
- Postman collection imported; `baseUrl` = `https://stage-platform.wellfit.com/treasury`.
- No Postman environment selected (use collection variables only).

**Expected**

- `GET /health` returns **200 OK**.
- Response body is **`Ok`**.

**Steps**

First send **GET** `/health` using the PAY-3821 Postman collection (no auth header required).

Then confirm HTTP status **200** and response body **`Ok`**.
