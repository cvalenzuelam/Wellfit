Validate that funding-batch SFTP failure sends one PAY-3821 alert email listing all unsent batches

**AC Linked:** PAY-3821 / AC-1, AC-2

**AC Description:** Exactly one PAY-3821 alert is delivered; body enumerates every unsent batch (Id + filename); not one email per batch.

**Preconditions**

- TC-05 completed; settle time elapsed.
- Access to QA inbox configured in TC-04.
- Optional: SendGrid Activity for delivery confirmation (accepted ≠ delivered).

**Expected**

- **Exactly one** email with subject indicating funding batch **send failed** (PAY-3821 alert).
- Body lists **all three** staged files: **`PAY3821-E2E-1.xml`**, **`-2.xml`**, **`-3.xml`** (with batch Ids).
- **Optional expected:** one additional coordinator **stage failed (Send)** email (PAY-3587) — not a PAY-3821 defect.

**Steps**

First open the QA inbox configured in TC-04.

Then locate the PAY-3821 **Funding Batch Send Failed** alert received after TC-05.

Next confirm **only one** PAY-3821 failure alert for this run (not three separate alerts).

Finally verify the email body enumerates **all three** unsent synthetic batches and save screenshot or forward as evidence.
