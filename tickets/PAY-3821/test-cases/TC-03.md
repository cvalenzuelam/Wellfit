Validate that synthetic pending FundingBatches rows can be staged for PAY-3821 testing

**AC Linked:** PAY-3821 / AC-2, AC-3 (test data for multi-batch failure)

**AC Description:** At least three pending batches exist in Platform DB with NULL send timestamp before the failure run.

**Preconditions**

- SQL access to STAGE Platform DB (`stage-platform-wellfit-sqlserver.database.windows.net`, database **Platform**).
- Prior PAY-3821 marker rows cleaned up if present.

**Expected**

- **3 rows** inserted with `BatchFileName` like `PAY3821-E2E-%` and `MerchantId` = **`E2E3821`**.
- All staged rows have **`RequestSentTimeStamp` IS NULL** before trigger.

**Steps**

First run cleanup/delete for prior `PAY3821-E2E-%` markers if any exist.

Then insert three synthetic rows (`PAY3821-E2E-1.xml`, `-2.xml`, `-3.xml`) with **`RequestSentTimeStamp` NULL**.

Next run a **SELECT** on `Payments.FundingBatches` filtered by `PAY3821-E2E-%`.

Finally confirm **3 rows**, all **`RequestSentTimeStamp` NULL**, and save row **Id** values as evidence.
