---
description: Release 9.6 STAGE regression — Postman layout, TokenVault/CNP status, resume context
alwaysApply: false
globs:
  - regression/9.6/**
---

# Wellfit QA — Release 9.6 STAGE regression

## Canonical Postman

Import **only** `regression/9.6/Regression-STAGE/` (collection **Regression - STAGE** + env **Regression STAGE**).

Modules: **CNP** · **TokenVault**. Do not reintroduce split CNP/TokenVault collections at `9.6/` root.

## Resume

Read `regression/9.6/SESSION-CONTEXT.md` first for status, hosts, SQL DBs, and open TokenVault work.

## Quick facts

- Processor tokens: numeric from `TokenVault.dbo.PaymentTokens` (see `scripts/cnp-payment-tokens-by-brand-STAGE.sql` + `cnp/` docs).
- TokenVault SQL Visualize requests are inside each TC folder (Platform vs TokenVault labeled).
- CP TokenVault TCs (05–07, 13): PARKED — physical lane / `cardToken`.
- `add-token` may return **Returned existing token.** (idempotent by processorToken) — still valid for TC08.
- If `/tokenvault` → **403 Site Disabled**: App Service `stage-wf-tokenvault-api` is Stopped → Start in Azure.
- Azure Data Studio: **Run** only — Estimated Plan → `SHOWPLAN permission denied` (not a product fail).
