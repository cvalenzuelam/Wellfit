---
description: PAY-4047 Stage QA — ach-returns nullable Charge fields; Jason inject + FileProcessedEvent runbook
globs: tickets/PAY-4047/**
alwaysApply: false
---

# PAY-4047 — Stage QA (PASS — Jason runbook)

**Status:** STAGE QA **PASS** (2026-07-08). Chris validated with Jason Slack runbook.

## Bug

AchReturns EF loads `[Payments].[Payments]` as `Charge`. Real ACH rows have **NULL** `Token` and `ApprovalNumber`. Pre-fix → `SqlNullValueException`. Fix: PR #305 — `string?` + `.IsRequired(false)`.

## Stage runbook (order)

1. **Anchor** — Platform SQL: ACH `'000'`, NULL Token/ApprovalNumber, `LEN(TransactionId) <= 20`, no existing `ReturnedPayments`
2. **Inject** — `tickets/PAY-4047/scripts/inject-ach-return.sql`: `@Commit=0` dry run → `@Commit=1` commit
3. **Trigger** — POST `stage-wf-payments-func` **`FileProcessedEvent`** (host key `default` from App keys); body `FileName` `ECheckReturnReport_*`, `ProcessedAt` UTC — **within 5 min** of SQL insert
4. **Verify** — `ReturnedPayments.ProcessingStatus` ≠ 0; App Insights 0 `SqlNullValueException`

**Not Stage:** Event Grid topic `operations` (local only). Deprecated: `wellfit-datafactory` CSV publish script.

## Postman

- Collection: `postman/collections/PAY-4047-FileProcessedEvent-STAGE.postman_collection.json`
- Env: `tickets/PAY-4047/PAY-4047-FileProcessedEvent-STAGE.postman_environment.json` — set `functionKey` (never commit)

## Pass criteria

- FileProcessedEvent **200** + `Payment.ReturnReport.Ingested`
- SQL `ProcessingStatus = 1` or `3`
- App Insights: no `SqlNullValueException` on ach-returns for test window

## Context

Full runbook: `tickets/PAY-4047/PAY-4047-QA-Context.md`. Dev CI: PR #305 — Jira only.
