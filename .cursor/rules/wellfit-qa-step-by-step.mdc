---
description: Step-by-step PAY ticket QA with Chris — slow pace, Spanish, STAGE context.
alwaysApply: true
---

# Wellfit QA — paso a paso con Chris

## QA owner

- **Name:** Chris (Christopher Ariel Valenzuela Medina)
- **Work email (alert inbox for STAGE QA):** `cvalenzuela@arkusnexus.com`
- When staging Treasury settlement alert recipients in Azure, use this address unless Chris specifies another.
- **Azure access:** Chris **has Azure portal access**. When QA is blocked by missing STAGE config (hosts, `accountId`, feature flags, App Settings, connection strings, Key Vault refs, etc.), **do not stop at “ask Brett only”** — guide him to look it up in Azure (App Service → Configuration / Identity / Logs). Prefer concrete portal path + what key to search for.
- **SQL access:** Chris **has database access to STAGE and Preprod** (`stage-platform-wellfit-sqlserver.database.windows.net` and the Preprod SQL server the team uses). When blocked on merchant/subMerchant, ACH anchors, tokens, returns, etc., give him **ready-to-run SQL** (Platform / Payments / TokenVault as needed) — do not assume he must wait on a DBA.

## Pace

- **One step only** per reply unless Chris asks for more.
- **Do not** dump full test plans, all ACs, or recovery steps upfront.
- Wait for confirmation before the next step.
- **Spanish** with Chris for step-by-step walkthrough (simple words).
- **Testmo + evidencia en el mismo ritmo:** when Chris is walking STAGE paso a paso, follow `wellfit-qa-step-by-step-testmo.mdc` — attach a Testmo-ready case + evidence checklist on verifiable steps (not only at the end).
- **Pass/fail emoji:** when he pastes a result, start with ✅ / ❌ / ⚠️ (see that Testmo rule).

## Team language (US)

- Chris’s **team / client is US-based**.
- **Slack team channels, Jira comments, Testmo cases, bug descriptions, and anything shared with the US team → English.**
- Do not draft Spanish for Slack/Jira/Testmo unless Chris explicitly asks for Spanish.
- Chat with Chris can stay Spanish; deliverables for the team stay English.

## Deployed build

- **Assume the ticket change is already deployed** in the target env when Chris asks to test.
- Do not gate on PR/commit/deploy checks unless something fails unexpectedly.

## STAGE Treasury (PAY-3821+)

- Postman: **No environment**; `baseUrl` = `https://stage-platform.wellfit.com/treasury`; header **`api_key`** (Treasury API collection pattern), not Bearer.
- SQL: `stage-platform-wellfit-sqlserver.database.windows.net` → `Platform`.
- Failure trigger: `processor.vantiv.batch:sftpurl` → `10.255.255.1` (restore `batch.vantivprelive.com` after).
- Alert recipients setting: `notifications:settlement:recipients`.
